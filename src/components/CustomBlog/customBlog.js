import React from "react";

const CustomBlog = () => {
    const h1style = {
        textAlign: "center",
        margin: "1rem"
    };

    return (
        <h1 style={h1style}>Hello, this is custom blog.</h1>
    );
};

export default CustomBlog;